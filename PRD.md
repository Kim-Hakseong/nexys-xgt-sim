# PRD.md — Nexys XGT Simulator

## 목적
XG5000이 실장비 없이 시뮬레이션 불가한 공백을 메운다: LabVIEW(마스터)가 실장비처럼 접속·읽기·쓰기할 수 있는 XGT PLC 시뮬레이터.

## P0
| ID | 기능 | 수용 기준 |
|---|---|---|
| X-01 | PLC 메모리맵 | %I/%Q/%M 영역, 비트(X)/바이트(B)/워드(W)/더블워드(D) 접근 뷰, IEC 주소 파서(%MW100, %IX0.2.5 등). 스레드 안전 |
| X-02 | I/O 구성 모델 | 베이스/슬롯/모듈(입력32·출력32·AD16 등) 정의 → 메모리 자동 매핑(슬롯당 점수는 spec/xgi-addressing.md 확정값, 기본 64점 가정) |
| X-03 | FEnet 서버 | TCP 서버(포트 설정, 기본값은 spec 기재), 멀티클라이언트, XGT 전용 프로토콜: 개별/연속 읽기·쓰기, 상태 응답 — **spec/xgt-fenet-reference.md 기재분만** |
| X-04 | 오류 응답 | 잘못된 주소/범위/타입 요청에 실장비와 동일한 에러 프레임(spec 기재) — "정확히 거절하는 것"도 실장비 역할이다 |
| X-05 | I/O 패널 UI | 디지털 입력 토글 그리드(슬롯2·3, 64점), 출력 LED 그리드(슬롯4 — LabVIEW가 쓴 값 표시), AD 채널 32개 값 입력(공학단위→raw 변환 옵션) |
| X-06 | 값 자동화 | AD 채널·입력 접점에 룰: 고정/증가/램프/사인/랜덤/토글 — tick 순수 함수(검증된 패턴) |
| X-07 | 트래픽 로그 | RX/TX raw hex + 해석 요약 + 타임스탬프, 에러 필터, 파일 저장 |
| X-08 | 프로젝트 파일 | I/O 구성+초기값+자동화 룰 저장/로드(.nxp JSON) |
## P1
| X-09 | Cnet 서버(RS-422/485) — spec/xgt-cnet-reference.md 게이트 | X-10 | 동작 룰: "%MW100에 1 쓰이면 t ms 후 %MX200 세트" 트리거→액션 (핸드셰이크 모사) | X-11 | 응답 지연/무응답 주입(견고성 시험) |

## 마일스톤
| M | 산출물 | DoD |
|---|---|---|
| M1 | 메모리맵+IEC 주소 파서 | 파서·접근 벡터 통과 |
| M2 | FEnet 프레임 코덱 (spec 게이트) | spec 기재 예제 프레임 벡터 + 1바이트 주입 불변 통과. spec 부재 시 ⛔ |
| M3 | FEnet 서버 e2e (테스트 클라이언트 왕복) | 읽기/쓰기/연속/오류 전 케이스 + 멀티클라이언트 |
| M4 | I/O 구성 모델+.nxp | 매핑·라운드트립 테스트 |
| M5 | UI: 랙 패널(토글·LED·AD 입력) | 스모크: 테스트 클라이언트로 쓴 값이 LED에, 토글이 읽기에 반영 |
| M6 | 값 자동화+트래픽 로그 | 제너레이터 벡터 + 스모크 |
| M7 | 캡처 픽스처 회귀 + (있으면) Cnet | fixtures 재생 전 케이스 응답 일치 |
| M8 | publish + 사용법 README + LabVIEW 접속 체크리스트 | publish 확인 후 삭제 |
