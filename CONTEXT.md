# CONTEXT.md — 이 프로젝트가 존재하는 이유

- 소유: Haku(김학성), 넥시스 시스템사업부 업무용 사내 도구. (개인 2막/Noktra 라인과 별개 — 브랜드 표기 없음)
- 배경: LabVIEW ↔ LS XGT PLC 연동 프로젝트가 완성되어 있으나, XG5000은 실장비 없이는 시뮬레이션이 불가능하다.
  실장비가 없는 환경에서 LabVIEW 애플리케이션을 검증하기 위해 **PLC의 역할을 대신하는 시뮬레이터**를 만든다.
- 핵심 요구: 더미/랜덤이 아니라 **실장비처럼 프로토콜에 정확히 응답**하는 강력·심플한 도구.
  대상 구성(XG5000 I/O 파라미터 기준): XGI CPU / 슬롯0 XGL-EFMT(B) FEnet / 슬롯1 XGL-C42A Cnet /
  슬롯2·3 XGI-D24A(DC입력 32점×2) / 슬롯4 XGQ-TR4A(TR출력 32점) / 슬롯5·6 XGF-AD16A(A/D 16채널×2). 주소체계 IEC(%IX/%QX/%MW).
- 방법론: 하네스 엔지니어링 (선례: Modbus Workbench — 동일 스택·동일 품질 기준). 골든 벡터 + spec 게이트(조작 제로).
- 검증의 왕도: 완성된 LabVIEW 코드가 이미 정답 요청 프레임을 생성한다 → 더미 리스너로 캡처해 픽스처화(장비 불필요).
