# LabVIEW 접속 체크리스트 — Nexys XGT Simulator

시뮬레이터를 실장비 대신 붙일 때 순서대로 확인한다.

> ## ⛔ 선결 조건 — 지금은 0장을 넘어갈 수 없다
>
> **FEnet 프레임 코덱이 없어 시뮬레이터가 접속을 받을 수 없다.**
> `spec/xgt-fenet-reference.md` 에 프로토콜 근거가 기재되지 않았기 때문이다(README "게이트를 푸는 방법" 참조).
> 1~5장은 코덱이 붙은 뒤에 유효하다. **0장은 지금 바로 할 수 있고, 오히려 지금 해야 한다** —
> 0장의 캡처가 코덱 구현의 근거가 된다.

---

## 0. 캡처 먼저 (장비 불필요 · 지금 가능 · 30분)

완성된 LabVIEW 코드는 이미 정답 요청 프레임을 만든다. 그것을 잡는 것이 이 프로젝트의 핵심 입력이다.

- [ ] 더미 리스너를 띄운다: `nc -l <포트> > fixtures/labview-capture/req_read.bin`
      (포트는 LabVIEW가 실장비에 접속할 때 쓰는 그 포트)
- [ ] LabVIEW 앱의 대상 IP를 `127.0.0.1` (또는 리스너 PC IP)로 바꾼다
- [ ] **읽기** 동작을 한 번 수행 → 리스너를 끊고 파일 크기가 0이 아닌지 확인
- [ ] 같은 방법으로 동작별 캡처를 모은다:
      `req_read.bin` / `req_read_continuous.bin` / `req_write.bin` / `req_write_continuous.bin`
- [ ] `xxd fixtures/labview-capture/req_read.bin` 으로 프레임을 눈으로 확인
- [ ] 각 요청의 **기대 응답**을 XGT FEnet 매뉴얼과 대조해 같은 이름 `.expected` 로 작성
      → ⚠️ 시뮬레이터 출력을 복사하면 안 된다. 회귀가 자기 자신을 검증하는 셈이 되어 무의미해진다
- [ ] `dotnet test` — 캡처가 자동 편입되어 "코덱이 없다"고 실패하면 정상 (게이트가 살아 있다는 증거)

캡처한 프레임은 헤더 레이아웃·명령 코드·Invoke ID 규칙을 역산하는 근거가 된다.
매뉴얼 발췌와 함께 쓰면 가장 확실하다.

---

## 1. 네트워크 도달

- [ ] 시뮬레이터 PC와 LabVIEW PC가 같은 서브넷인지 확인 (`ipconfig` / `ifconfig`)
- [ ] LabVIEW PC → 시뮬레이터 PC `ping` 성공
- [ ] 시뮬레이터의 **바인딩 IP**를 정한다
      - 같은 PC에서 테스트: `127.0.0.1`
      - 다른 PC에서 접속: `0.0.0.0` (모든 NIC) 또는 해당 NIC의 IP
      - ⚠️ `127.0.0.1` 로 바인딩하면 다른 PC에서 접속할 수 없다 — 접속 실패의 가장 흔한 원인
- [ ] **포트**를 실장비와 동일하게 맞춘다
      ([미정] 기본 포트는 spec 미기재라 시뮬레이터에 기본값이 없다 — 매뉴얼 확인 후 입력)
- [ ] Windows 방화벽에서 해당 포트 인바운드 허용 (또는 `Nxs.App.exe` 허용)
- [ ] 실장비가 같은 IP로 아직 살아 있지 않은지 확인 (IP 충돌 시 증상이 혼란스럽다)

## 2. 시뮬레이터 기동

- [ ] `Nxs.App.exe` 실행 — 메인 윈도우가 뜬다
- [ ] 바인딩 IP·포트 입력 → **[시작]**
- [ ] 상태 필이 와인레드 **"수신 중 <IP:포트> · 접속 0"** 으로 바뀐다
- [ ] "랙 구성" 탭에서 슬롯별 할당 주소가 XG5000 I/O 파라미터와 일치하는지 대조
      → 불일치하면 `.nxp` 의 `addressing.slotPoints` 를 실제 값으로 맞춘다

## 3. 접속

- [ ] LabVIEW 앱에서 연결 → 상태 필의 접속 수가 **1**로 증가
- [ ] "트래픽 로그" 탭에 RX 행이 나타난다 (raw hex + 해석 요약)
- [ ] 해석 요약의 주소가 LabVIEW가 의도한 주소와 같은지 확인
      → 다르면 주소 산법(슬롯 스트라이드) 불일치다. 2장 마지막 항목으로 돌아간다

## 4. 읽기 — 시뮬레이터 → LabVIEW

- [ ] "디지털 입력" 탭에서 슬롯2 점 0을 토글 ON
- [ ] LabVIEW 화면의 해당 입력이 ON으로 바뀐다
- [ ] 점 31까지 몇 개를 눌러 비트 순서가 뒤집히지 않았는지 확인 (엔디안·비트오더 검증)
- [ ] "A/D 입력" 탭 슬롯5 CH00에 공학단위 값 입력 (예: `5`)
      → raw 칸이 자동 변환되고(예: `2000`), LabVIEW에 같은 값이 보인다
- [ ] 스케일이 실제 모듈 설정과 맞는지 확인 (`.nxp` 의 `analogChannels`)

## 5. 쓰기 — LabVIEW → 시뮬레이터

- [ ] LabVIEW에서 출력 접점을 ON
- [ ] "디지털 출력" 탭 슬롯4의 해당 LED가 와인레드로 채워진다
- [ ] LabVIEW에서 OFF → LED가 빈 원으로 돌아온다
- [ ] "트래픽 로그"에 쓰기 요청 RX + 응답 TX가 쌍으로 남는다

## 6. 오류 동작 — "정확히 거절하는 것"도 실장비 역할이다

- [ ] LabVIEW에서 범위를 벗어난 주소를 읽어 본다
- [ ] 트래픽 로그에 **오류 행**(ErrorBrush)이 뜨고 사유가 표시된다
- [ ] **연결이 끊기지 않고** 유지된다 → 이어서 정상 요청이 성공한다
- [ ] LabVIEW가 실장비와 같은 에러 코드를 받는지 매뉴얼과 대조
      (⛔ 와이어 에러 코드 매핑은 코덱 구현 시 결정된다)

## 7. 견고성

- [ ] LabVIEW 앱을 강제 종료 → 접속 수가 0으로 줄고 시뮬레이터는 계속 수신한다
- [ ] 재접속 → 정상 동작
- [ ] LabVIEW를 2개 이상 동시 접속 → 응답이 섞이지 않는다
- [ ] 스캔 주기를 최소로 낮춰 장시간(10분+) 폴링 → UI 멈춤·메모리 증가 없음
      (트래픽 로그는 5000행 링 버퍼 — 넘치면 "용량 초과로 N건 버림"이 표시된다)
- [ ] "값 자동화"를 켠 상태로 폴링 → LabVIEW 화면 값이 룰대로 움직인다

## 8. 마무리

- [ ] **[프로젝트 저장]** 으로 현재 구성·초기값·자동화 룰을 `.nxp` 로 남긴다
- [ ] **[트래픽 로그 저장]** 으로 검증 근거를 파일로 남긴다
- [ ] 실장비와 같은 결과가 나오지 않은 항목은 `spec/` 근거와 대조해 원인을 기록한다

---

## 증상별 원인

| 증상 | 먼저 볼 것 |
|---|---|
| 접속 자체가 안 된다 | 바인딩 IP가 `127.0.0.1` 인지 / 방화벽 / 포트 불일치 |
| 접속은 되는데 응답이 없다 | 트래픽 로그에 RX가 오는지 → 오면 코덱 문제, 안 오면 네트워크 |
| 응답은 오는데 값이 이상하다 | 주소 산법(슬롯 스트라이드) 불일치 → "랙 구성" 탭 대조 |
| 값이 절반만 맞다 / 뒤집힌다 | 워드 엔디안·비트 오더 → 디지털 입력 점 0/1/15/16 을 개별 확인 |
| AD 값이 배수로 틀리다 | `analogChannels` 스케일이 실제 모듈 설정과 다르다 |
| 연결이 자꾸 끊긴다 | 프레이밍 위반 (트래픽 로그의 Note 행 확인) — 요청 프레임 길이 필드 해석 문제 |
| 시작 버튼이 눌리지 않는다 | ⛔ 코덱 부재. 화면 상단 배너의 안내를 따른다 |
