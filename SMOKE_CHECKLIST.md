# 스모크 체크리스트 — Nexys XGT Simulator

배포 전 Windows PC에서 실제 exe로 확인한다. LabVIEW 접속 절차는 `LABVIEW_CHECKLIST.md` 를 쓴다.

> ⛔ **FEnet 코덱이 없어 서버를 켜는 항목(3장)은 현재 수행할 수 없다.**
> 0~2장과 4~7장은 코덱과 무관하게 지금 전부 수행 가능하다 — 게이트는 서버에만 걸려 있고
> 랙 패널·자동화·프로젝트 파일은 정상 동작한다.

## 0. 기본

- [ ] `Nxs.App.exe` 더블클릭 실행 — 설치/런타임 없이 메인 윈도우가 뜬다
- [ ] 창 크기 조절/최대화 시 랙 그리드·로그 레이아웃이 깨지지 않는다
- [ ] 창 폭을 줄여도 점 그리드가 줄바꿈되며 스크롤로 접근 가능하다

## 1. 비주얼 (DESIGN Rev.B 이식 확인)

- [ ] 배경이 웜 뉴트럴 그레이지 그라데이션(#F4F3F0 → #ECEAE5 → #DDDBD3)
- [ ] 강조색이 와인 레드(#7A1020) 하나뿐 — 녹색/파란색 등 팔레트 외 색이 없다
- [ ] 주소·hex·값이 모두 고정폭(Menlo/Consolas)으로 보인다
- [ ] 입력 점 토글 ON = 와인레드 배경 + 흰 글자, OFF = 연한 카드색
- [ ] 출력 LED ON = 와인레드 채운 원, OFF = 회색 테두리 빈 원
- [ ] 탭 선택 시 잉크(거의 검정) 필 + 흰 글자

## 2. 랙 패널 (서버 없이 가능)

- [ ] ⛔ 배너가 상단에 뜨고 `spec/xgt-fenet-reference.md` 경로와 해제 조건을 안내한다
- [ ] [시작] 버튼이 비활성 상태다
- [ ] "랙 구성" 탭에 슬롯 0~6이 모두 보이고, 슬롯0·1은 "프로세스 데이터 없음"으로 표시된다
- [ ] "디지털 입력" 탭에 슬롯2·3이 각 32점씩 나온다 — 점에 마우스를 올리면 주소 툴팁(`%IX512`)
- [ ] 점을 토글해도 앱이 멈추지 않는다 (32점 전체를 빠르게 눌러 본다)
- [ ] "디지털 출력" 탭 LED 32개가 모두 OFF 상태다 (사용자가 클릭해도 바뀌지 않는다)
- [ ] "A/D 입력" 탭 슬롯5·6에 16채널씩 나오고 각 채널에 주소(`%IW80`)가 표시된다
- [ ] AD 채널에 `5` 입력 → raw 칸이 자동 변환된다 (스케일 설정 시)
- [ ] AD 채널에 `abc` 입력 → 빨간 오류 문구가 뜨고 메모리는 바뀌지 않는다
- [ ] AD 채널에 `12.` 까지 입력한 상태로 1초 이상 기다려도 입력칸이 되돌려지지 않는다

## 3. 서버 (⛔ 코덱 필요)

- [ ] 바인딩 IP·포트 입력 → [시작] → 상태 필이 "수신 중"으로 바뀐다
- [ ] 잘못된 IP 입력 → 오류 메시지만 표시되고 앱은 정상 동작 유지
- [ ] 사용 중인 포트 입력 → "바인딩 실패" + 원인 표시
- [ ] [정지] → 상태 필이 "정지"로 돌아오고 접속이 모두 닫힌다
- [ ] 정지 후 다시 [시작] → 재바인딩된다

## 4. 값 자동화

- [ ] 자동화 룰이 없는 프로젝트에서 [자동화 시작] → "자동화 룰이 없습니다" 안내
- [ ] 룰이 있는 `.nxp` 를 열면 "값 자동화" 탭에 목록이 뜬다
- [ ] 각 룰에 주소·종류(램프/사인/…)·주기·스케일·tick 0..7 미리보기가 보인다
- [ ] [자동화 시작] → 해당 탭의 AD 값/입력 점이 룰대로 움직인다
- [ ] 룰 체크박스를 끄면 그 룰만 멈추고 나머지는 계속 움직인다
- [ ] [자동화 정지] → 전부 멈춘다

## 5. 트래픽 로그

- [ ] 서버 트래픽 발생 시 RX/TX 행이 시각·연결·해석·raw hex 와 함께 쌓인다
- [ ] "오류만" 체크 → 정상 행이 사라지고 오류 행만 남는다
- [ ] "일시정지" 체크 → 표시가 멈춘다. 해제하면 그동안 쌓인 것이 나타난다
- [ ] [비우기] → 목록이 비고 요약이 0건이 된다
- [ ] 오래 켜 두면 요약에 "용량 초과로 N건 버림"이 표시된다 (조용히 사라지지 않는다)
- [ ] [트래픽 로그 저장] → 파일 다이얼로그 → 저장된 파일에 시각·방향·요약·hex 가 들어 있다

## 6. 프로젝트 파일

- [ ] 입력 점 몇 개를 켜고 [프로젝트 저장] → `.nxp` 파일이 생긴다
- [ ] 메모장으로 열어 사람이 읽을 수 있는 들여쓰기 JSON 인지 확인
- [ ] 앱을 재시작하고 [프로젝트 열기] → 켰던 점이 그대로 복원된다
- [ ] 자동화 룰의 on/off 상태도 복원된다
- [ ] 깨진 JSON 파일을 열면 오류 메시지만 뜨고 앱은 계속 동작한다
- [ ] `formatVersion` 을 큰 수로 바꾼 파일을 열면 "지원하지 않는 formatVersion" 안내가 뜬다

## 7. 종료

- [ ] 서버 실행 중에 창을 닫아도 프로세스가 남지 않는다 (작업 관리자 확인)
- [ ] 자동화 실행 중에 창을 닫아도 프로세스가 남지 않는다
